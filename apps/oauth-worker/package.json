{
  "name": "@otr-discord-bot/oauth-worker",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "bunx wrangler deploy",
    "dev": "bunx wrangler dev",
    "start": "bunx wrangler dev",
    "cf-typegen": "rm ./worker-configuration.d.ts && bunx wrangler types"
  },
  "dependencies": {
    "@otr-discord-bot/shared": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251217.0",
    "wrangler": "^4.55.0"
  }
}
