/** TTL of an OAuth attempt in miliseconds. */
export const OAUTH_ATTEMPT_TTL = 600 * 1000;

/** Request header containing a timestamp. */
export const TIMESTAMP_HEADER = 'X-Timestamp';

/** Request header containing authorization signature. */
export const SIG_HEADER = 'X-Signature';
